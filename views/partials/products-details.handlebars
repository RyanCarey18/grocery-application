              <div class="col-md-3 mt-4  margin-bottom">
                <div class="showProduct" >
                  <div class="pic"><img src="/assets/img/foodsIcons/{{product_name}}.png" class="img-fluid" alt=""></div>
                  <div class="product-info">
                    <h4>{{product_name}}</h4>
                    <span>{{price}} $</span>
                    <p> Aisle: {{aisle.aisle_name}}</p>
                    <p>Department : {{department.department_name}}</p>
                    <div class="AddProduct">
                      <button id="add-btn" class="add-btn" data-id="{{id}}" data-toggle="modal" data-target="#myModal"><i class="ri-menu-add-line"></i></button>
                    </div>
                  </div>
                </div>
              </div>